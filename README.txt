* user can set dynamic port by running - Windows 'set FLASK_RUN_PORT = portnumber?' Linux 'export FLASK_RUN_PORT = portnumber?'   

* Application is in incomplete state.
    - Model's are created as per given 
        1) User 
        2) Adress

* Two function are made
    - For fetching all user. 
    - For fetching single user with the help of unique public id.
    - all output is in JSON form.